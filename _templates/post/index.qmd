---
title: "{{title}}"
description: "One sentence for the listing card: the question and the answer."
date: "{{date}}"
categories: [Snapshot]   # first entry is the post type: Snapshot (under 6 min) or Deep dive; then 2 to 3 topics
image: images/thumbnail.png
draft: true
---

```{r}
#| label: setup
#| include: false

# Anchor here() to this post so paths work no matter where R starts
here::i_am("posts/{{slug}}/index.qmd")

# House colours, ggplot2 theme, and the packages every post uses
source(here::here("R", "theme_cwr.R"))

# Data download bundle helpers; data_bundle_version is the release version linked at the end.
# Bump it when the data changes and /publish rebuilds the bundle.
source(here::here("R", "data_bundle.R"))
data_bundle_version <- 1

# Post-specific packages go here, and in R/packages.R
```

```{r}
#| label: load-data

# Small tidy files produced by R/02_clean_data.R live in data/
# example <- read_csv(here("posts", "{{slug}}", "data", "example.csv"))
```

<!-- Opening paragraph: the question this post asks and the one-line answer. -->

## First finding as a plain-language heading

<!-- One or two paragraphs, then one chart. -->

```{r}
#| label: fig-first
#| fig-cap: "Caption shown under the chart."
#| fig-alt: "What the chart shows, for screen readers."
#| fig-height: 5
#| fig-width: 8

# Pick a template from the cwr-charts skill and build the chart here.
```

## Second finding

```{r}
#| label: tbl-first
#| tbl-cap: "Caption shown under the table."

# gt table here (one table per chunk).
```

## Data and methods

<!-- Where the data came from (links), what was excluded and why, any caveats
     a careful reader should know. Full source table is in README.md. -->

::: {.callout-note collapse="true"}
## Reproducibility and data download

Code and data for this post: [`posts/{{slug}}/`](https://github.com/gpberber/chartingwaterlooregion/tree/master/posts/{{slug}}) on GitHub. See [Reproduce a post](../../reproduce.qmd) for the steps.

**Download the data:** [`r cwr_bundle_name("{{slug}}", data_bundle_version)`](`r cwr_bundle_url("{{slug}}", data_bundle_version)`) holds every table used in this post as CSV and Parquet files, an Excel workbook, the data dictionary below, and a README with sources and licences.

```{r}
#| label: tbl-dictionary
#| tbl-cap: "Data dictionary for the tables used in this post."

# Driven by data/tables.csv and data/dictionary.csv; /review-post checks they are complete
cwr_dictionary_table("{{slug}}")
```

```{r}
#| label: session-info

cwr_session_info()
```
:::
